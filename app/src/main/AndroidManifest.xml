<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tap.taskassigningandplanning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".NavigationBottomActivity"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".Login"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".Registration"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".utils.activities.Activities"></activity>
        <activity
            android:name=".utils.activities.ActivityClicked"
            android:parentActivityName=".utils.activities.Activities" />
        <activity android:name=".utils.activities.ActivityClickedSearch" />
        <activity android:name=".utils.team.TeamSearch" />
        <activity android:name=".RatingActivity" />
        <activity
            android:name=".utils.activities.Task.ActivityTask"
            android:parentActivityName=".utils.activities.ActivityClicked" />
    </application>

</manifest>